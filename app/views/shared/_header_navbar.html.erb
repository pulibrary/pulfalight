<header class="lux">
  <library-header app-name="<%= application_name %>" app-url="<%= root_path %>">
    <div class="pull-right menu--level-1 col-xs-12 col-sm-6">
      <ul>
        <%= render partial: 'shared/header_account_dropdown' %>
      </ul>
    </div>
  </library-header>
  <% unless params[:action] == 'error' || params[:action] == 'generate' %>
    <div class="header__secondary">
      <nav class="container" role="navigation">
        <div class="row">
          <div id="search-navbar" class="search-navbar" role="search">
            <%= render_search_bar %>
          </div>
        </div>
      </nav>
    </div>
  <% end %>
</header>
