<%= form_tag aeon_external_request(document).url, method: :post, class: 'al-request-form' do |f| %>
  <%= render_hash_as_hidden_fields(aeon_external_request(document).form_mapping) %>
  <button type='submit' class='lux-button solid small lux-expanded'>
    <%= t('arclight.request.container', type: document.level.downcase) %>
  </button>
<% end %>
